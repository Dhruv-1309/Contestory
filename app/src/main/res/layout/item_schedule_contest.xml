<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="5dp"
    app:cardBackgroundColor="@color/bg_card"
    app:cardCornerRadius="18dp"
    app:cardElevation="1dp"
    app:strokeColor="@color/border"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="14dp">

        <!-- Circular Platform Logo Container -->
        <ImageView
            android:id="@+id/platformLogo"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:background="@drawable/bg_notification_circle"
            android:importantForAccessibility="no"
            android:padding="7dp"
            android:scaleType="centerInside"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_leetcode" />

        <!-- Contest name -->
        <TextView
            android:id="@+id/contestName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/text_primary"
            android:textSize="14.5sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/contestTime"
            app:layout_constraintStart_toEndOf="@id/platformLogo"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Codeforces Round 900" />

        <!-- Platform label below name -->
        <TextView
            android:id="@+id/platformName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:textColor="@color/text_secondary"
            android:textSize="11.5sp"
            app:layout_constraintStart_toStartOf="@id/contestName"
            app:layout_constraintTop_toBottomOf="@id/contestName"
            tools:text="Codeforces" />

        <!-- Start time chip -->
        <TextView
            android:id="@+id/contestTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_badge"
            android:backgroundTint="@color/bg_countdown"
            android:fontFamily="monospace"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:textColor="@color/accent_purple"
            android:textSize="12.5sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/bellButton"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="20:00" />

        <!-- Bell toggle button -->
        <ImageButton
            android:id="@+id/bellButton"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginStart="6dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/desc_toggle_reminder"
            android:padding="8dp"
            android:scaleType="centerInside"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_bell_on"
            tools:visibility="visible" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
